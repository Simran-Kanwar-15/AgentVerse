# AgentVerse Backend Environment Template
# Copy this file to '.env' and fill in your custom values.

# Fernet symmetric encryption key for secure user API keys (Generate with: cryptography.fernet.Fernet.generate_key().decode())
FERNET_KEY=your-fernet-key-here

# Secret key for JWT Authentication signing
JWT_SECRET=your-jwt-signing-secret-here
